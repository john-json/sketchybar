local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
    topmost = "window",
    height = 28,
    color = colors.transparent,
    padding_right = 5,
    padding_left = 5,
    margin = 50,
    corner_radius = 8,
    y_offset = 8

})
