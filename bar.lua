local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
    topmost = "window",
    height = 20,
    color = colors.transparent,
    padding_right = 10,
    padding_left = 10,
    margin = 0,
    corner_radius = 9,
    y_offset = 8

})
